{
  "name": "cloud-disk-cpp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "latest"
  }
}
